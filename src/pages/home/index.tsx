const HomePage = () => {
    return (
        <div>
            home page but different
            home page but different
            home page but differentA
        </div>
    );
};

export default HomePage;